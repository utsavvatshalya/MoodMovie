
  # Integrate UI with Backend

  This is a code bundle for Integrate UI with Backend. The original project is available at https://www.figma.com/design/zO0ATRCV6u4pvGG9dwFQHK/Integrate-UI-with-Backend.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  